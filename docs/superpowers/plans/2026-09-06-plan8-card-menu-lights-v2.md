# Plan 8: Card menu, side-button semantics, Lights v2

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** spec §4.12. The knob stops scrolling cards and always adjusts the page's value; the side button gains click = card menu / hold = Treadmill (instant stop while the belt is active is unchanged); the Lights cards are rebuilt as swipeable pages (Brightness, Kelvin, Colour presets) with tap-to-switch-on and hold-to-switch-off; `DialUi.cpp` is split so the new drawing lands in its own files.

**Verification for every task:** `pio test -e native`, `pio run -e devkit-usb`, `pio run -e dial-usb`; no new `src/` warnings. Commit per task. `pio` is at `~/Library/Python/3.9/bin/pio`. Never stage `src/config.h`.

**Constraints:** `doc/Q1_BLE_NOTES.md` behaviour unchanged; `TreadmillHandler` untouched. Loop task never touches sockets. `DialUi`: no `delay`, no per-frame heap, wrap-safe time, colours via the palette (`col(Col)` / `DialTheme`), `thickLine()` not `drawWideLine`; true-colour pixels only ever go straight to `M5Dial.Display` after the canvas push, into rectangles/circles the canvas reserved as `TRANSPARENT` (the airline-logo pattern). Pure logic Arduino-free with Unity tests in `[env:native]`. HA side unchanged (`doc/HA_LIGHTS_AUTOMATIONS.md` contract stands). Flights auto-show (§4.11) keeps working; menu selection and hold-home count as manual navigation.

---

## Task 1: Input events and `CardMenu` (pure, TDD)

**Files:** `src/DialInput.h/.cpp`, `test/test_dial_input/test_main.cpp`; new `src/CardMenu.h/.cpp`, new `test/test_card_menu/test_main.cpp`; `platformio.ini` native filter (+`CardMenu.cpp`).

- [ ] `DialInput::tick(...)` gains two inputs after `btnClicked`: `bool btnSingleClicked, bool btnHold` (fed from `M5Dial.BtnA.wasSingleClicked()` / `wasHold()` in Task 3). `DialEvents` gains `bool btnClick = false;` (decided single click) and `bool btnHold = false;`. `btnStop` keeps meaning the instant `wasClicked` edge. Both new inputs count as activity for the backlight. Tests: single click → `btnClick` only on that tick; hold → `btnHold`; `btnStop` still fires on the raw click; either wakes a dimmed backlight.
- [ ] `CardMenu` (pure): ring order `TREADMILL, CLOCK, FLIGHTS, LIGHT_OFFICE, LIGHT_LAMP` (from `CardId`, `COUNT` excluded — `static_assert(CardId::COUNT == 5)`). API: `void open(CardId current, uint32_t nowMs)`, `void close()`, `bool isOpen() const`, `CardId highlight() const`, `void detents(int n, uint32_t nowMs)` (wraps, refreshes idle timer), `CardId select()` (returns highlight, closes), `bool tick(uint32_t nowMs)` (returns true and closes after `kIdleCloseMs = 8000` without input), and geometry for hit-testing shared with the view: `static constexpr int kRingRadius = 88, kItemRadius = 18, kItemRadiusHighlight = 22, kHitRadius = 26;` `static void itemCentre(uint8_t index, int& x, int& y)` (index 0 at 12 o'clock, clockwise, centre (120,120)); `static int8_t hitTest(int x, int y)` (item index or −1). Tests: opens on current; detents wrap both ways; select returns highlight and closes; idle close at 8 s exactly, refreshed by a detent; itemCentre for index 0 = (120,32) and index 1 ≈ (204,93); hitTest centre/margin/miss.
- [ ] Commit: `Add side-button click/hold events and CardMenu (pure, tests)`.

## Task 2: `LightCardState` v2 and `LightLayout` (pure, TDD)

**Files:** `src/LightCardState.h/.cpp` (rewrite), `test/test_light_card_state/test_main.cpp` (rewrite), new `src/LightLayout.h` (header-only, replaces `src/LightButtons.h` — delete it), `src/LightsModel.h` (if `Command` needs nothing new, leave it).

- [ ] `LightLayout.h`: constants from spec §4.12 — power-off circle (120,124) r 44; small power glyph (120,212) r 8, hit r 14; page dots y 54, spacing 12, r 3; brightness caption y 160; kelvin value y 112, bar x 52..188 y 170 h 8; swatch ring radius 74, r 12 / 16 selected, hit r 18, first at 12 o'clock; centre disc r 30. `static constexpr float kPresetHues[8] = {0,30,60,120,180,240,275,320};` `void swatchCentre(uint8_t i, int& x, int& y)`; `int8_t hitSwatch(int x, int y)`; `bool hitPowerGlyph(int x, int y)`; `uint8_t nearestPreset(float hue)`.
- [ ] `LightCardState` v2: `enum class Page : uint8_t { BRIGHT, KELVIN, COLOUR }`; ctor `explicit LightCardState(bool hasColour)`; `uint8_t pageCount() const` (3 or 2); `Page page() const`; `void resetPage()` (→ BRIGHT); `void swipe(int dir)` (−1/+1, clamped, no wrap); `void sync(const LightsModel::LightState&, uint32_t nowMs)` (keep Plan 6's confirm-hold logic: held fields survive a contradicting sync until echoed or 1.5 s); `LightsModel::Command tapOn(uint32_t nowMs)` (only when `!on`: POWER on, optimistic `on = true`, page → BRIGHT); `LightsModel::Command powerOff(uint32_t nowMs)` (POWER off, drops any pending settle); `void detents(int n, uint32_t nowMs)` (page BRIGHT ±5 clamp 1–100; KELVIN ±100 clamp [min,max]; COLOUR ±1 preset wrapping; each arms the 300 ms settle; ignored when `!on`); `void selectPreset(uint8_t i, uint32_t nowMs)` (COLOUR page tap; arms settle); `LightsModel::Command tick(uint32_t nowMs)` (settle → BRIGHT / TEMP / HUE(preset hue, sat 100) command; no idle release any more); `bool settling() const`; `const LightsModel::LightState& view() const`; `uint8_t preset() const` (nearest to view().hue, or the pending one); `bool kelvinLive() const` (`view().mode != HS`), `bool colourLive() const` (`mode == HS`); `float ringFraction() const` (brightness/100). Tests: page navigation and clamping; resetPage; detents per page incl. wrap/clamp; ignored while off; tapOn from off returns POWER on and lands on BRIGHT; powerOff drops a pending settle; settle emits exactly one command 300 ms after the last detent with the right shape (HUE carries the preset hue and sat 100); confirm-hold kept (stale echo within 1.5 s doesn't clobber; matching echo ends the hold); nearestPreset (hue 250 → index of 240; hue 350 → index of 0).
- [ ] Commit: `LightCardState v2: swipeable pages, presets, tap-on/hold-off; LightLayout replaces LightButtons`.

## Task 3: `DialUi` — theme split, menu, knob/side-button semantics, Lights v2 drawing

**Files:** new `src/DialTheme.h` (move `Col` enum + palette table + `col()` out of `DialUi`), new `src/DialGlyphs.h/.cpp` (device-only: `drawPowerGlyph, drawSunGlyph, drawColourDotsGlyph, drawBeltGlyph, drawClockGlyph, drawPlaneGlyph, drawBulbGlyph(LovyanGFX&, int cx, int cy, int r, uint32_t colour)` from circles/arcs/lines/triangles only — mirror the SVG glyphs in the design artifact), new `src/DialMenuView.h/.cpp` (`void drawCardMenu(LovyanGFX&, const DialTheme&, const CardMenu&)`), new `src/DialLightsView.h/.cpp` (`void drawLightCard(LovyanGFX&, const DialTheme&, const LightCardState&, const char* name, bool mqttUp, float holdProgress)` plus `void paintLightTrueColour(LGFX_Device&, const LightCardState&)` for the post-push swatches/disc), `src/DialUi.h/.cpp`, `src/CardRing.h` (unchanged API), `src/main.cpp` (only if constructor args change — they should not).

- [ ] Theme split first (pure refactor, build must stay green): `DialTheme` holds `useCanvas` and `col(Col)`; `DialUi` owns one and forwards. Move the existing light and flights drawing constants only where needed.
- [ ] Input semantics in `handleInput` (belt idle = screen is a card screen or MENU): `ev.btnStop` while RUNNING/STARTING/CONNECTING → stop as today; otherwise ignored. `ev.btnHold` → close menu/selector, `releaseLights` (no longer needed — remove `releaseLightCards`; instead `resetPage()` on the light cards), `m_autoShow.noteManualNavigation()`, `m_cards.set(TREADMILL)`, accept beep. `ev.btnClick` (belt idle): menu closed → `m_menu.open(m_cards.current(), nowMs)`; menu open → select highlighted and navigate. Knob: MENU → `m_menu.detents`; SELECTOR → as today; RUNNING → nudge speed as today; FLIGHTS → cycle aircraft by `ev.detents` (wrap, `noteManualNavigation`); LIGHT_* → `card.detents(...)` (only when on); CLOCK/DISCONNECTED → nothing. Tap: MENU → `hitTest` on a glyph selects that card, elsewhere selects the highlight; LIGHT_* → off: `tapOn`; on: `hitPowerGlyph` → `powerOff`, COLOUR page `hitSwatch` → `selectPreset`, else nothing; FLIGHTS → cycle (as today); others as today. Long press (touch): LIGHT_* on → `powerOff` with accept beep (the hold arc already draws from `holdProgress`; make sure it draws on light screens); LIGHT_* off → nothing; others as today. Swipe: LIGHT_* on → `card.swipe(dir)`; SELECTOR as today. Navigating onto a light card (menu select, hold-home leaving, auto-show) → `resetPage()`.
- [ ] Screens: add `Screen::MENU` (wins over card screens when `m_menu.isOpen()`, loses to CONNECTING/STARTING/RUNNING/SELECTOR which close it). `FrameKey`: `menuOpen`, `menuHighlight`, light `page`, `preset`, `settling` (replace the removed engaged/settling fields); `currentScreen()` mapping unchanged otherwise.
- [ ] Drawing: `drawCardMenu` per spec §4.12; `drawLightCard` pages per spec (dots, brightness ring + Font7 %, kelvin value + 3-step bar using three new palette entries `WARM, NEUTRAL, COOL` added to `Col` in the free slots 12–14 with RGB `#FFB46B #FFF1D6 #CFE3FF`, colour ring with TRANSPARENT holes for the eight swatches + centre disc, small power glyph, `not active - turn to use` caption, `waiting for HA` / `no data` states kept). `render()`: after the canvas push, if the visible screen is a light card on the COLOUR page, `paintLightTrueColour` fills the swatches (`hsv→rgb565`, r 12 / 16) and the centre disc directly on `M5Dial.Display` (no sprite; ~9 `fillCircle` calls). Selected swatch outline (white, amber while settling) stays in the canvas. Kelvin page with the colour-live Lamp, and Colour page with kelvin-live, draw dim per spec.
- [ ] Remove: `LightButtons.h` usage, `releaseLightCards`, `lightCardFor` engaged logic, knob card-scrolling, the `tap: next`-era Flights knob no-op. Keep `DialUi.cpp` under ~1500 lines after the split.
- [ ] Commit: `DialUi: card menu, click/hold side button, knob-as-value everywhere, Lights v2 pages (theme/glyph/view split)`.

## Task 4: Docs and Phase H checklist

**Files:** `README.md` (Controls + Desk mode + Lights sections rewritten for §4.12), `doc/AUDIT_2026-09-03.md` (Phase F marked superseded like Phase E; new "## Phase H hardware checklist (menu + Lights v2)"), spec §4.12 as-built amendment, `doc/HA_LIGHTS_AUTOMATIONS.md` (one line: Dial UI changed, contract unchanged).

- [ ] Phase H items: click opens the menu with the current card highlighted, knob wraps, tap-on-glyph and tap-elsewhere both select, 8 s auto-close; hold-home from every card; side click still stops the belt instantly while running; Lamp off → tap anywhere turns on at last brightness on the Brightness page; knob brightness with one HA trace per burst; swipe to Kelvin and back, 100 K steps clamped at 2000/6535 (Office 2202/4000); Colour page shows eight true-colour swatches, knob wraps, tap selects, HA shows the hue; kelvin page dim while in colour mode and vice versa; touch-hold 1 s turns off with the red arc; small power glyph turns off; flights knob cycles aircraft; auto-show still returns to Clock; heap ≥ 100 KB free at rest.
- [ ] Commit: `Docs: card menu and Lights v2`.
