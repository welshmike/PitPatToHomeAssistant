# PaceKeeper code audit (pre M5Stack Dial migration)

Date: 2026-09-03. Scope: working tree at commit 24ca1bc plus ~630 lines of uncommitted changes.
Build check: `pio run -e usb` succeeds. Flash 82.9% of the 1.25 MB OTA slot, RAM 25.9%.

## What is in good shape

- Clear module split: `ba05protocol` (wire format), `TreadmillHandler` (BLE + session), `TreadmillState` (NVS totals + calibration), `MqttView` (HA discovery + publish), `main.cpp` (glue).
- Q1 BLE behaviour is well understood and documented in `doc/Q1_BLE_NOTES.md`. The kick-phase, supervision-timeout drop, zombie detection and GATT-cache reuse are all hard-won and should be preserved verbatim through the migration.
- All MQTT publishes already run on the main loop via the `m_newDataAvailable` flag. That is the right shape for adding a display.

## Findings, ordered by impact on the migration

### 1. BLE stops whenever WiFi or MQTT is down (correctness, belt safety)

`loop()` returns early when WiFi is disconnected or `connectToMqtt()` fails, and `treadmill.handle()` is only reached after both succeed. Keepalives must go out every 200 ms against a 6 s supervision timeout. A broker restart or a WiFi blip longer than 6 s mid-walk drops the BLE link, and the pad kills the motor when BLE drops (the "thrown off" case the project's own notes describe).

`setup()` also spins forever waiting for WiFi, so with no WiFi the device never reaches BLE at all, and the 300 s watchdog then reboots it in a loop. On the Dial the whole point of local controls is to work without Home Assistant, so this must change.

Fix: call `treadmill.handle()` unconditionally every loop; turn WiFi and MQTT into non-blocking state machines with their own backoff; drop the forced restart on WiFi loss.

### 2. `TreadmillHandler` mixes four concerns and repeats the delta maths four times

`TreadmillHandler.cpp` (852 lines) plus its header (440 lines, with large inline `onDisconnect`) hold: BLE link management, Q1 kick-phase heuristics, session accounting (connection baselines, pause snapshot, resume suppression), the idle/pause timers, and NVS settings. The "current minus connection base, clamp at zero, derive steps" block appears in `onDisconnect`, pause-stop, session-end and live-totals.

Why it matters now: the Dial needs the same session data for the screen, and a second command source. Extract:

- `SessionTracker` (or a `computeDelta()` helper) owning baselines, pause snapshot and the four delta calculations.
- Keep `TreadmillHandler` as the BLE link layer only.

### 3. Command handling is duplicated in `main.cpp` and will be duplicated again by the Dial

Each MQTT handler does: call the treadmill, build an optimistic `TreadMillData`, publish it. Encoder and button input will need the identical sequence. Speed constants are scattered: raw factor 1600 and cap 6080 in `main.cpp`, 3.8 mph in `mqttview.cpp`, `START_SPEED` 994 in the handler.

Fix: a `TreadmillController` intent layer with `start()`, `pause()`, `stop()`, `adjustSpeed(delta)`, `setSpeed(mph)` that clamps, applies optimistic state, and notifies all views (MQTT and display). Constants move to `platform.h`.

### 4. Shared state is not protected (latent, more likely with a display)

`notifyCallback` runs on the NimBLE task and writes the ~80-byte `m_lastData` struct field by field. `getLastData()` copies it from the main loop. The `volatile bool` flags are fine, the struct copy is not: a display refreshing at 10 to 30 Hz raises the odds of a torn read (for example speed from the new packet, status from the old).

Also `m_state.addSession()` performs an NVS write from inside the NimBLE callback (in `onDisconnect` and `notifyCallback`), blocking the BLE host task for milliseconds.

Fix: guard `m_lastData` with a FreeRTOS mutex or a sequence-counter double buffer; queue session commits to the main loop. The `m_publishingConfigs` guard in `MqttView` is now dead (all publishes are on one task) and its comment is misleading; remove it.

### 5. Main loop runs at 10 Hz

`delay(100)` at the end of `loop()`. Acceptable for MQTT, too slow for encoder and button feel, and adds up to 100 ms jitter to the 200 ms keepalive. Remove the delay; rate-limit the display redraw instead.

### 6. Toolchain is too old for the Dial

Installed: espressif32 platform 5.4.0 (Arduino-ESP32 2.0.x), which has no `m5stack-stamps3` board. Registry latest is 7.1.0; 6.13.0 is the last of the 6.x line. Two PlatformIO cores are installed (6.1.16 in `~/Library/Python/3.9` and 6.1.19), which PIO itself warns about. Libraries in use: NimBLE-Arduino 2.5.1 (supports S3), ArduinoJson 7.4.3, PubSubClient 2.8, mqttdisco (git). M5Dial library is 1.0.3 and depends on M5Unified and M5GFX.

### 7. Board-specific pins are hard-coded

`LED_BLE_PIN 2` in `platform.h`. On the Dial GPIO2 is PORT.B. Introduce a `board.h` selected by build flag so the DevKit build keeps working during the transition.

### 8. Uncommitted work

Nine files changed and uncommitted: idle-disconnect timer, pause timeout, pending-command queue, restore-calibration topic, step-length entity removed, platformio.ini restructured into `usb` and `ota` envs. `partitions.csv` is untracked but referenced by `platformio.ini`, so a fresh clone does not build. `Archive.zip` is untracked and should be deleted or ignored. Commit this before any migration work so there is a known-good DevKit baseline to bisect against.

### 9. Small items

- `handleSpeedCommand` calls `atof` on the raw MQTT payload, which PubSubClient does not NUL-terminate. Copy into a bounded buffer first. The `String((char*)payload).substring(0, length)` pattern elsewhere has the same over-read.
- `TreadMillData::fwVersion` is always 0 but still published.
- `VERSION "2026.1.1"` is static; consider deriving from git.
- Flash at 82.9%: adding M5GFX and fonts will exceed the 1.25 MB slot on the DevKit. The Dial has 8 MB, so use a 2 x 3 MB OTA partition table there.

## M5Stack Dial v1.1 hardware facts relevant to the design

- MCU: StampS3A, ESP32-S3FN8, dual core 240 MHz, 8 MB flash, no PSRAM. WiFi and BLE coexist on S3 the same way as on the classic ESP32.
- Display: 1.28 inch round 240x240, GC9A01 over SPI (RS G4, MOSI G5, SCK G6, CS G7, RST G8, backlight G9).
- Touch: FT3267 capacitive, I2C (SDA G11, SCL G12, INT G14).
- Encoder: quadrature on G41 (A) and G40 (B), 16 detents and 64 pulses per revolution. The encoder has no push switch.
- Buttons: WAKE (BtnA in M5Unified, GPIO42) and RST on the body. The screen is not a mechanical button.
- Buzzer on G3. RTC BM8563 and RFID WS1850S on the internal I2C bus.
- PORT.A (I2C) G13/G15, PORT.B (GPIO) G1/G2. HOLD pin GPIO46 must be driven high to stay on when battery powered.
- Power: USB-C 5 V, rear screw terminal 6 to 36 V, or 3.7 V battery connector.
- Library: `m5stack/M5Dial` 1.0.3 exposes `M5Dial.Display` (M5GFX), `Touch`, `BtnA`, `Encoder` (PJRC interrupt encoder, `read()` / `readAndReset()`), `Speaker`, `Rtc`, `Rfid`, `Power`. `begin(cfg, enableEncoder, enableRFID)`.
- v1.1 vs v1.0: StampS3A module with improved antenna, larger tactile buttons, lower power. Pinout unchanged.

## Sources

- https://docs.m5stack.com/en/core/M5Dial%20V1.1
- https://docs.m5stack.com/en/core/M5Dial
- https://github.com/m5stack/M5Dial (src/M5Dial.h, library.json)
- https://github.com/m5stack/M5Unified (board_M5Dial pin table)
- https://openelab.io/blogs/learn/difference-between-m5stack-dial-and-m5stack-dial-v1-1
- https://docs.platformio.org/en/latest/boards/espressif32/m5stack-stamps3.html

## Phase A hardware checklist (DevKit)

Real-belt verification for the `devkit-usb` build, to be run by Mike before tagging
`phase-a-verified` and starting Plan 2 (Dial port + UI):

1. Flash `devkit-usb`. HA shows the same PaceKeeper device and entities; retained totals unchanged.
2. Start / pause / resume / stop from HA. Session summary and totals match a manual delta.
3. Set speed from HA slider; stop via slider at 0.
4. Mid-walk WiFi outage 30 s: belt keeps running, HA catches up.
5. Boot with WiFi off: belt connects; HA appears when WiFi returns.
6. Idle disconnect and pause timeout still fire (set to 1 min each for the test).
7. Kick phase from cold connect settles in about 2 min as before.
8. With a walk running, stop the MQTT broker for 60 s then restart it — belt keeps running, HA recovers within one backoff period.
9. After WiFi returns from an outage, confirm `esp32-<chipid>.local` still resolves for OTA.

When all nine pass, tag `phase-a-verified` and start Plan 2 (Dial port + UI).

## Phase C hardware checklist (Dial)

Real-belt verification for the `dial-usb`/`dial-ota` build, to be run by Mike once Plan 2 is
implemented, before calling the Dial migration done.

1. Each screen appears in the right state: Disconnected (last-session summary), Selector (start
   speed picker), Connecting (attempt count), Starting (pulsing "STARTING" during belt
   countdown), Running, Paused (dimmed Running with pulsing label) — walk through all six and
   confirm each one only shows for its own state.
2. Tap the screen while Disconnected: opens the Selector, showing the candidate speed at the
   configured default (see item 17) on the speed ring — the belt does not start yet.
3. Tap while Running: belt pauses, screen shows Paused.
4. Tap while Paused: belt resumes at the previously commanded speed, not the minimum.
5. Hold the screen for about 1 second while Running or Paused: belt stops.
6. Press the side WAKE button on any screen (Disconnected, Selector, Connecting, Starting,
   Running, Paused): on Running/Paused, belt stops (emergency stop, always); on Selector, the
   picker closes with no belt command sent; on Connecting/Disconnected this is a harmless
   refusal since there's no belt link yet, and it does not cancel an in-progress connect; only
   tap/hold on the Connecting screen do that.
7. Rotate the dial 5 clicks while Running: exactly one speed command goes out, about 0.4 s after
   the last click, changing speed by 0.5 mph (5 x 0.1 mph); the centre overlay shows the target
   speed during the wait and reverts to elapsed time about 1.5 s after the last click.
8. Rotate the dial while stopped, paused, connecting or disconnected (Selector not open):
   nothing happens (no command, no overlay). Rotate-to-start was removed 2026-09-03.
9. Idle power: leave the screen on Disconnected/Stopped for 2 minutes — brightness drops to
   20%; leave it for 10 minutes total — backlight goes off. The first tap/turn/hold after either
   only wakes the screen; the belt does not react to that first input.
10. Buzzer tones: one short click on an accepted tap, two short beeps on stop, and a low buzz
    when a command is refused (e.g. during the post-connect cooldown, or a disconnect attempt
    while the belt is still active).
11. Home Assistant still shows the same `PaceKeeper` device and entities, and retained totals
    are unchanged, after switching from the DevKit to the Dial.
12. `pio run -e dial-ota -t upload` succeeds against `pacekeeper-dial.local` after an initial
    USB flash.
13. Kick phase from a cold connect settles (no BLE drop/reconnect loop) in about 2 minutes, same
    as the DevKit.
14. Mid-walk: turn the WiFi AP off for 30 s, then stop the MQTT broker for 60 s (with the belt
    still running throughout) — the belt keeps running through both outages and HA catches up
    once WiFi/MQTT return.
15. The Connecting screen shows an incrementing attempt count while retrying (once there's been
    at least one attempt — otherwise just "Connecting..." with no count), and a tap or a long
    press during Connecting cancels the connect attempt instead of trying to start/stop a belt
    that isn't running yet. A queued start from Disconnected (via the Selector's tap/hold) shows
    Connecting first if the belt isn't already reachable, then Starting, then Running.
16. The hold-to-stop/cancel progress arc appears on every screen — holding on the Connecting
    screen fills the red arc the same as holding on Running does. Status dots are hidden on the
    Running screen and visible on all others (including Paused and Selector).
17. Selector: opens on tap from Disconnected, candidate starting at the configured Start Speed
    default (1.0 mph out of the box). Knob detent and horizontal swipe (right/clockwise = faster)
    each step the candidate by ±0.2 mph, clamped to 0.6–3.8 mph. Tap confirms — the belt starts
    at the candidate speed shown. Hold starts the belt at the default speed instead, skipping the
    candidate. The side button cancels with no belt command sent (see item 6). 20 seconds with no
    input closes the Selector back to Disconnected, also with no belt command.
18. Long press directly on the Disconnected screen (without opening the Selector first): belt
    starts immediately at the configured default speed.
19. Home Assistant Start button: pressing it starts the belt at the configured Start Speed value,
    not a fixed speed.
20. Change Start Speed from Home Assistant (e.g. to 1.4 mph), reboot the board, and start the
    belt again (from HA or the Dial's hold gesture) — it starts at the new value, confirming the
    setting persisted across the reboot.

## Phase D hardware checklist (desk mode)

Real-hardware verification for the card ring and Clock card (spec 4.8), to be run by Mike once
Plan 4 is implemented, before calling desk mode done.

1. Power on with WiFi available: the Dial boots straight into the Clock card, not the Treadmill/
   Disconnected card.
2. Clock accuracy: once WiFi/NTP has come up, the displayed time is correct local time (`Europe/
   London` by default, or whatever `TIMEZONE_TZ` is set to) to within a minute.
3. Power-cycle the Dial with WiFi unavailable (e.g. AP off, or WiFi disabled in `config.h`): the
   Clock still boots to the correct time immediately, sourced from the RTC rather than showing
   `--:--` or a stale/wrong time.
4. Knob scroll: with the belt idle and no selector open, rotate the knob one detent at a time —
   it moves Treadmill → Clock and Clock → Treadmill, in both directions (clockwise and
   counter-clockwise), with no other cards to skip over yet.
5. Side button homing: from the Clock card, press the side WAKE button — it returns to the
   Treadmill card without sending any belt command.
6. Belt-screen override: start the belt from the Treadmill card while the Clock card is
   showing... i.e. scroll to Clock, then start the belt via Home Assistant (or return to
   Treadmill and start) — Connecting/Starting/Running/Paused screens override whichever card was
   showing, and once the belt returns to idle the ring resumes on the card that was showing
   beforehand.
7. Dimming: leave the screen idle on the Clock (or Treadmill) card for 2 minutes — brightness
   drops to 20% and stays there; leave it idle much longer (10+ minutes) and confirm the screen
   never turns off. The first tap/turn/hold after dimming only wakes the screen — it does not
   scroll the ring, home, or send a belt command.
8. Treadmill flows unchanged: with the ring on the Treadmill card, the existing tap (selector),
   hold (start), pause, and stop gestures all behave exactly as documented in the Phase C
   checklist above — desk mode must not have altered any belt-control behaviour.

## Phase E hardware checklist (flights)

**Superseded 2026-09-05** by Phase G (flights via HA); items referring to AirlineCodes, `waiting
for WiFi` or the configured radius no longer apply.

Real-hardware verification for the Flights card (spec 4.9), to be run by Mike once Plan 5 is
implemented, before calling the Flights card done.

1. Scroll the ring to Flights with real aircraft within the configured radius: the card shows the
   nearest one first, with its route and, for a major airline (e.g. BA/Virgin), its logo — not the
   operator-name/callsign text fallback.
2. Tap the screen: it cycles to the next aircraft (nearest first, wrapping after the last one back
   to the first), updating logo/callsign/route/altitude/speed/distance together.
3. Find or wait for an aircraft whose airline isn't in `AirlineCodes.cpp`'s ICAO→IATA table: the
   card falls back to the operator name (or callsign if the operator is also unknown) as text
   instead of a logo, rather than showing a blank or broken image.
4. With the card showing and fetching normally, pull WiFi (disable the AP, or step out of range):
   the previous aircraft list keeps showing with a small grey stale dot near the top once the next
   fetch times out, then — once the Dial itself notices the AP is gone — the card switches to
   "waiting for WiFi". Re-enable the AP: the card recovers on its own (no reboot or manual nudge
   needed), the stale dot clears, and aircraft data resumes.
5. Late at night, or with the radius reduced, when nothing is nearby: the card shows "no aircraft
   nearby" with the configured radius underneath, not stale data or a blank screen.
6. Leave the Dial on the Flights card for 30 minutes: `ESP.getFreeHeap()`, logged at `log_i` on
   every fetch, stays above 60 KB free throughout (check the serial/OTA log, not just the last
   line).
7. Scroll the knob away from Flights to another card, wait, then scroll back: fetching stops
   within 60 seconds of leaving the card (confirm via the log — no further `FlightsService` GET
   lines once the free-delay window elapses) and resumes once the card is showing again.
8. With the belt connected and a walk running (or start/stop from HA/the Treadmill card), scroll
   to Flights while it's actively fetching: belt connect, start and stop all behave exactly as
   documented in the Phase C checklist — a fetch in progress on the network task must not delay or
   block a belt command on the loop task.
9. After the Dial has been on the Flights card long enough to see a new airline's logo for the
   first time: a corresponding `/logos/{IATA}.png` file appears (check via a debug listing, or
   infer from a `FlightsService` log line noting the download), and a `log_i` line records the
   download; scrolling away and back (or a reboot) shows that airline's logo from the cache without
   re-downloading it.

## Phase F hardware checklist (lights)

**Superseded 2026-09-06** by Phase H (menu + Lights v2); items referring to `Power`/`Bright`/
`Colour` buttons, engage/release, the 10 s release timeout, or the knob scrolling the card ring no
longer apply.

Real-hardware verification for the Office and Lamp cards (spec 4.10, `doc/HA_LIGHTS_AUTOMATIONS.md`),
to be run by Mike once Plan 6 is implemented, before calling the Lights cards done.

1. Power-cycle the Dial with both lights already known to HA (either state): scroll to Office and
   Lamp without tapping anything — each card shows the light's real current state (brightness/on/
   off, colour caption) straight from the retained MQTT topic, not `no data` or a stale guess.
2. Tap `Power` on Office, then on Lamp, from both HA-off and HA-on starting states: each light
   physically switches within about a second, and the card's `Power` button and centre value follow
   HA's own state change (not just the optimistic local flip).
3. Tap `Bright` to engage it, turn the knob several detents quickly, then stop: exactly one
   `light.turn_on` call with the final `brightness_pct` lands 300 ms later — check the automation
   trace for `automation.pacekeeper_dial_light_commands` in HA (Settings → Automations → this
   automation → Traces) and confirm one run per knob burst, not one run per detent.
4. On the Lamp card, tap `Colour`: first tap engages temperature or hue (whichever HA currently
   reports), a second tap switches to the other, a third tap releases — turning the knob at the
   kelvin extremes confirms it clamps at 2000 K and 6535 K rather than wrapping or overshooting.
5. Engage `Bright` (or `Colour`) and leave the knob alone for 10 seconds: engagement releases on
   its own (the button un-fills) with no further command sent.
6. With nothing engaged on a light card, turn the knob: it scrolls the card ring (Office ↔ Lamp ↔
   Flights ↔ …) exactly like the other desk cards, not the light's own value.
7. Pull WiFi (disable the AP, or step out of range) while a light card is showing: the card
   switches to `waiting for HA`, and `Power`/`Bright`/`Colour` all go inert. Re-enable the AP: the
   card recovers on its own (no reboot needed) and shows real state again once MQTT reconnects and
   the refresh round-trip completes.
8. Leave the Dial on a light card (or scrolling between the two) for a while at rest: `ESP.getFreeHeap()`,
   logged at `log_i` as `Heap: free=... minFree=... largestBlock=... ble=...`, stays at or above
   90 KB free (check the serial/OTA log, not just the last line).
9. With the belt connected and a walk running (or start/stop from HA/the Treadmill card), scroll to
   a light card and operate its buttons/knob: belt connect, start, pause and stop all behave exactly
   as documented in the Phase C checklist — MQTT light commands/state on the network task must not
   delay or block a belt command on the loop task.

## Phase G hardware checklist (flights via HA)

Real-hardware verification for the Flights card's Home Assistant data path (spec 4.11,
`doc/HA_FLIGHTS.md`), to be run by Mike once Plan 7 is implemented, before calling flights-via-HA
done.

1. Power-cycle the Dial with the retained `pacekeeper-dial/flights/state` message already sitting
   on the broker (i.e. Home Assistant has published at least once since its own last restart):
   scroll to Flights immediately — the card shows the retained aircraft list straight away, not
   "no aircraft nearby" while it waits for the next Flightradar24 poll.
2. Compare the card against `sensor.flightradar24_current_in_area`'s `flights` attribute (Developer
   Tools → States) at the same moment: same aircraft, same order (nearest first), same route/
   altitude/speed/distance/bearing, within the automation's own refresh cadence.
3. With a major airline overhead (e.g. BA/Virgin) not yet in
   `input_text.pacekeeper_dial_logos_cached`: its logo appears on the card within one publish of the
   *PaceKeeper Dial: airline logos* automation — check the automation's trace for the
   `downloader.download_file` call and the resulting file under `config/www/logos/`.
4. Find or wait for an airline IATA code that isn't a real code, or one `pics.avs.io` doesn't have
   a logo for: the card falls back to the operator name (or callsign if the operator is also
   unknown) as text, not a blank or broken image; `input_text.pacekeeper_dial_logos_cached` still
   grows to include the code, so it isn't retried every scan.
5. While the belt is idle and the Clock card is showing, wait for (or trigger, e.g. flight-tracking
   an approaching aircraft) the aircraft count going from 0 to > 0: the ring switches to Flights on
   its own; once the count returns to 0, the ring returns to Clock on its own, without a knob
   scroll or side-button press in between.
6. In Home Assistant, turn the `Flights Auto-show` switch off, then repeat step 5: the ring no
   longer auto-shows or auto-returns — Flights only appears when scrolled to manually. Turn the
   switch back on and confirm auto-show resumes.
7. Leave the Dial on the Flights card (with aircraft cycling through logo fetches) for 30 minutes:
   `ESP.getFreeHeap()`, logged at `log_i` as `Heap: free=... minFree=... largestBlock=... ble=...`,
   stays at or above 100 KB free throughout (check the serial/OTA log, not just the last line), and
   shows none of the ~45 KB dips a TLS handshake used to cause — the new logo fetch is a plain HTTP
   GET of a few KB.
8. With the belt connected and a walk running (or start/stop from HA/the Treadmill card), scroll to
   Flights while it's actively fetching a logo: belt connect, start, pause and stop all behave
   exactly as documented in the Phase C checklist — the plain-HTTP logo fetch on the network task
   must not delay or block a belt command on the loop task.

## Phase H hardware checklist (menu + Lights v2)

Real-hardware verification for the card menu, side-button semantics and Lights v2 pages (spec
4.12), to be run by Mike once Plan 8 is implemented, before calling the menu/Lights v2 rewrite
done.

1. From an idle card (Treadmill, Clock, Flights, Office or Lamp), click the side button: the card
   menu opens with the five cards as glyphs on a ring, the card you were just on highlighted amber
   with its name in the centre — not always defaulting to Treadmill.
2. With the menu open, turn the knob one detent at a time: the highlight moves glyph to glyph and
   wraps past both ends (Treadmill → Lamp with one detent one way, Lamp → Treadmill the other).
3. Tap directly on a glyph: the ring switches to that card and the menu closes. Reopen the menu,
   then tap anywhere else on the face, or click the side button again: the ring switches to
   whichever glyph is currently highlighted — the menu is never a dead end.
4. Open the menu and leave it alone: after 8 seconds with no input it closes on its own, and the
   ring stays on the card it was on when the menu opened.
5. From every card (Treadmill, Clock, Flights, Office, Lamp, and with the menu itself open), hold
   the side button for about half a second: the ring jumps straight to the Treadmill card every
   time, closing the menu or an open selector on the way.
6. Start the belt (from HA, or the Treadmill card's hold gesture) and, while it's Running, click
   the side button: the belt stops instantly on the raw press, not after the ~500 ms (M5Unified's
   multi-click/hold threshold) decision window used elsewhere for the card menu.
7. With the Lamp off, tap anywhere on its card: the light turns on at whatever brightness it was
   last at (not a fixed value), and the card lands on the Brightness page.
8. On the Brightness page with the light on, turn the knob several detents quickly then stop:
   exactly one `light.turn_on` call with the final `brightness_pct` lands 300 ms later — check the
   automation trace for `automation.pacekeeper_dial_light_commands` in HA (Settings → Automations
   → this automation → Traces) and confirm one run per knob burst, not one run per detent.
9. Swipe from Brightness to Kelvin and back: the page changes each time (a swipe does not wrap past
   either end), and the knob now adjusts kelvin in 100 K steps, clamped at the light's own minimum
   and maximum (2000 K / 6535 K for most bulbs; 2202 K / 4000 K for the Office group) — turning past
   either end holds at the limit rather than wrapping or overshooting.
10. On the Lamp's Colour page: eight swatches in their true colours are drawn on a ring; turning the
    knob moves the selection one preset per detent, wrapping past both ends; tapping a swatch
    directly selects it. In Home Assistant, confirm the light's reported hue matches the selected
    preset once the 300 ms settle has landed.
11. On the Lamp, select a colour preset, then swipe to the Kelvin page: it draws dim with "not
    active - turn to use" until the first detent switches it live; do the reverse (set a kelvin
    value, then swipe to Colour) and confirm the Colour page dims the same way until a swatch is
    tapped or the knob is turned.
12. On a lit light card, touch and hold anywhere on the face for about a second: the light turns
    off, with the same red hold-arc progress the belt's hold-to-stop uses; tapping the small power
    glyph at the bottom of the card also turns it off immediately.
13. On the Flights card, turn the knob: it cycles to the next aircraft, same as tapping the screen —
    confirm both knob and tap advance the same list in the same order.
14. With `Flights Auto-show` on, let an aircraft come into range while the Clock card is showing:
    the ring auto-shows Flights and returns to Clock once the aircraft leaves range, exactly as
    before Plan 8 — confirm the new card menu and knob-as-value changes haven't broken auto-show's
    own return-to-Clock behaviour.
15. Leave the Dial at rest on a light card (or idling with the menu open) for a while:
    `ESP.getFreeHeap()`, logged at `log_i` as `Heap: free=... minFree=... largestBlock=... ble=...`,
    stays at or above 100 KB free throughout (check the serial/OTA log, not just the last line).
16. On a light card, swipe to Kelvin or Colour and leave it alone: it returns to Brightness after
    10 s (`PAGE_IDLE_MS`); a turn or tap in between restarts the 10 s.

Addendum (Plan 9, spec 4.13): the Flights card's empty state.

17. With the Flights card showing and no aircraft in range (not offline), confirm the radar sweep:
    three range rings and a crosshair, a rotating sweep line completing one revolution roughly every
    3 s with two trailing wedges fading behind it, and "Searching" underneath — not the old static
    "no aircraft nearby" text. Confirm it keeps sweeping while left showing, stops redrawing (a still
    frame) the moment an aircraft appears or you leave the card, and that a stale empty list still
    shows the small grey dot alongside the radar. Confirm `waiting for HA` still wins when MQTT is
    down, radar and all.

## Phase I hardware checklist (BLE connect hold)

Real-hardware verification for the connect scheduling around WiFi bring-up (spec 4.15), to be run
by Mike once Plan 11 is implemented. Read the log over MQTT while doing this — subscribe to
`pacekeeper-dial/diag` (see `doc/DIAGNOSTICS.md`); the INFO filter now forwards every `Treadmill:`
line, so the whole connect sequence is visible.

1. With the belt switched on, reboot the Dial (OTA upload, or the side button through a power
   cycle) and watch `pacekeeper-dial/diag`: `BLE connect hold on` appears while WiFi/MQTT come up,
   then `Net status 4` (MQTT_UP), then `BLE connect hold off` about 6 s later — the first
   `Connecting to ...` line must come after the hold-off line, i.e. at least ~6 s after
   `Net status 4`, not on top of the discovery burst.
2. In that same connect: `Service found on attempt 1`, `Write characteristic initialized`, `Early
   keepalive sent` and `Subscribed to notifications` all appear, with no `Service not found, retry
   n/8` line and no `disconnect()`/kick in between.
3. Leave it alone for 10 minutes after that connect: no kick cycle in the log (no repeated
   `Connecting to ...` / disconnect pairs), and the treadmill card still shows connected.
4. Power the Dial up with WiFi unavailable (router off, or the AP out of range) and the belt on:
   the treadmill still connects on its own. The hold bites only while the association attempt is
   outstanding — `BLE connect hold on`, then `WiFi association timed out` after 15 s
   (`kWifiConnectTimeoutMs`), then `BLE connect hold off` and the connect — so expect the belt
   connected inside ~20 s, and never a Dial that stays held with no network.
5. With the hold active (during the first seconds after an MQTT connect), press Connect on the Dial
   or send the connect command from HA: the connect starts immediately — a user-requested connect
   is never held. Confirm the log shows `Connecting to ...` without waiting for `BLE connect hold
   off`.
6. Over a normal session, confirm `BLE connect hold on` / `off` each appear once per transition and
   not once per loop — the log should stay readable.
7. Crash forensics (spec 4.16). Force a reset that is neither a power cycle nor a clean restart:
   the easiest is to cut the belt's power in the middle of a connect (the log has just shown
   `link begin` or `setup begin`) and leave the Dial to hit the 300 s loop watchdog, or hold the
   Dial's reset with power applied. When it reconnects, confirm the retained `diag/boot` record
   shows a `reset` other than `POWERON`/`SW` (`TASK_WDT`, `PANIC`, …) with a matching
   `"last_lines":N`, and that the retained `diag/last` object carries the same `reset` and `build`
   with N lines, oldest first, ending with the last line the firmware logged before it froze — a
   `link begin` or `setup begin` with no matching `link up`/`setup end` is the answer we are after.
   Both topics are retained, so `mosquitto_sub … -t 'pacekeeper-dial/diag/#' -C 2` works at any
   time afterwards. Then confirm it is a one-shot: bounce the broker (or the Dial's WiFi) so MQTT
   reconnects, and the new `diag/boot` record must say `"last_lines":0` while `diag/last` is left
   unchanged (a clean boot never erases the previous crash record). Finally, reboot the Dial over
   OTA and confirm `"reset":"SW"`, `"last_lines":0` — an OTA flash is not a crash.
8. Confirm the stall detector is quiet in normal use: over a session, `loop stall N ms` should not
   appear at all (spec 4.17 made the connect non-blocking, so not even around a connect). A
   stall line during ordinary running is a real finding — note what came before it in the log.

## Phase J hardware checklist (staged BLE connect)

Real-hardware verification for the staged connect (spec 4.17), to be run by Mike once Plan 13 is
flashed. Read the log over MQTT throughout — subscribe to `pacekeeper-dial/diag` (see
`doc/DIAGNOSTICS.md`). The connect brackets have changed: `connect() begin`/`connect() end` and
`discover begin`/`discover end` are gone, replaced by `link begin`, `link up after N ms`,
`link timeout after N ms, cancelled`, `setup begin` and `setup end`.

1. With the belt switched on, reboot the Dial (OTA upload, or the side button through a power
   cycle) and watch the connect: `link begin`, then `link up after N ms` with N well under 1500,
   then `setup begin`, `Service found on attempt 1`, `Write characteristic initialized`, `Early
   keepalive sent`, `Subscribed to notifications`, `Negotiated BLE params: ...`, `setup end` and
   `Connection successful.` — in that order, with no `Service not found, retry n/3` line and no
   disconnect in between.
2. In that same boot, confirm no `loop stall N ms` line appears anywhere around the connect. The
   old blocking `connect()` and the 2 s discovery loop were the two stalls the detector saw; both
   are gone.
3. Switch the belt OFF and leave the Dial trying to reach it for a few minutes. Expect a
   `link begin` … `link timeout after 6000 ms, cancelled` pair roughly every 11 s (6 s link
   timeout inside the 5 s attempt spacing), and **no** `Disconnected reason=...` line from our own
   disconnect. Throughout, the Dial's UI must stay live — turn the knob, open the menu, switch
   cards; nothing may freeze for a second at a time as it used to.
4. Confirm the Connecting screen still counts: while the belt is off, the Dial shows
   "Connecting… attempt N" with N incrementing once per `link begin`, and pressing Connect from HA
   or the Dial resets it to 1.
5. Cancel while connecting: with the belt off and the Dial mid-`link begin`, press Disconnect on
   the Dial (or send the disconnect command from HA). Expect `Connect cancelled by user while
   connecting`, no `Disconnected reason=...` from us, and no further `link begin` lines until you
   ask for a connect again.
6. Power-cycle the belt while connected: the Dial should reconnect on its own within ~15 s with a
   single `link begin` / `link up` / `setup end` sequence and no kick cycle (no repeated
   connect/disconnect pairs) in the following 10 minutes.
7. Walk a real session end to end (start, change speed, pause, resume, stop) and confirm nothing
   about the belt behaviour changed: keepalive-per-notification, the post-connect cooldown on the
   first speed command, the idle-disconnect timer and the pause timeout all behave as before.

## Phase K hardware checklist (hue wheel)

Real-hardware verification for the Colour page hue ring (spec 4.18), to be run by Mike once Plan 14
is flashed. Watch `pacekeeper-dial/light/lamp/set` (e.g. MQTT Explorer) alongside.

- [ ] Lamp on, swipe to the Colour page: a full-spectrum ring fills the outer band, red at 12 o'clock,
      green at about 4 o'clock, blue at about 8 o'clock; marker sits on the lamp's current hue; centre
      disc shows that colour. No card name, dots or power glyph.
- [ ] Tap the ring at 3 o'clock: marker jumps there with an amber outline, `hs_color [90,100]` is
      published within ~0.3 s, the lamp goes green, the outline turns white when HA echoes.
- [ ] Three clockwise detents: one command `hs_color [135,100]` after the last detent; marker moves
      45° clockwise.
- [ ] Anticlockwise past 12 o'clock wraps (e.g. 5 → 0 → 355).
- [ ] Put a finger on the ring and drag it a quarter turn: the marker and centre disc track the
      finger with no page swipe and no hold-to-off arc; one `hs_color` command about 0.3 s after the
      finger stops, another after it lifts if it moved again.
- [ ] Drag from the ring inward across the centre disc: the marker keeps following the finger's
      angle (radius does not matter once the ring has the gesture).
- [ ] Tap just inside the ring (about 75 px from centre) and just outside (about 118 px): both count.
      Tap the centre disc: beep, back to Brightness (spec 4.18 amendment), no lamp command sent.
- [ ] Change the lamp's colour from HA: the marker follows to the new hue within a second.
- [ ] Set the lamp to a colour temperature from the Kelvin page, swipe to Colour: the ring is two dim
      circles with the "not active - turn to use" caption; one detent brings the colour ring back.
- [ ] Office card: Brightness and Kelvin pages only, nothing changed.
- [ ] 10 s without input on the Colour page returns to Brightness.
- [ ] Heap line in the log (`Heap:` every 15 s) is unchanged before and after the Colour page.
- [ ] Spin the knob fast on the Colour page: the marker keeps up and the ring never tears or flickers.
- [ ] On a dim Colour page (lamp in colour-temperature mode), tap the ring at 3 o'clock: colour mode
      comes back at hue 90, exactly as a detent would (the two dim circles show where the ring is).
- [ ] Swipe from the centre of the face to leave the Colour page; a swipe that starts on the ring
      scrubs instead.
- [ ] Swipe once from Brightness: Colour page (dots show the middle dot lit); swipe again: Kelvin.
      Office: one swipe reaches Kelvin.
- [ ] On the Colour page tap the centre disc: beep, back to Brightness, no lamp command sent.

## Phase L hardware checklist (calendar)

Real-hardware verification for the Calendar card (spec 4.19), to be run by Mike once Plan 17 is
flashed.

- [ ] Card appears after Lamp in the card menu ring (Treadmill → Clock → Flights → Office → Lamp → Calendar).
- [ ] Within 10 s of boot once WiFi connects, the card shows the next real meeting with the correct
      London time and countdown timer.
- [ ] Add a fake meeting 6 minutes out to the Google Calendar; the Dial nudges the Clock card 5 min
      before (i.e. 1 min from now), then returns to Clock 1 min after the start time.
- [ ] While the nudge is showing the Calendar card, tap the screen: it returns to Clock immediately,
      and that same meeting does not nudge again (the dismiss is persistent for that meeting).
- [ ] With the belt running or paused, a nudge never appears over the Treadmill/Running/Paused screens.
- [ ] Stopping the relay (`launchctl bootout gui/$(id -u)/com.pacekeeper.calendar-feed`) shows
      `no calendar` within 30 min; `launchctl bootstrap` brings it back within 5 min (or `waiting for
      calendar` before the first successful fetch after power-on).
- [ ] Leave the Dial on the Calendar card and log the serial output: `Heap:` line (logged every 15 s)
      stays above 80 KB free over a 10-fetch span (50+ min).
- [ ] With the belt connected to the treadmill, scroll to the Calendar card and force ten fetches (e.g.
      shorten the poll interval, or manually trigger via MQTT); the belt link never drops (`MQTT
      disconnected` does not appear in the diag log) and belt control remains responsive.
- [ ] Follow-up lines at y 176/192/208 and the all-day/Tomorrow lines are trimmed to fit the circular
      bezel — no clipped characters at the edges.
- [ ] MQTT connection stays up throughout the ten-fetch sequence (no `MQTT disconnected` in the diag
      stream during `pacekeeper-dial/diag` logging).
